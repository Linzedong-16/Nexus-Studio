# Quickstart：验证 ER 图分析功能

**目的**：手动验证本功能端到端可用，覆盖 spec.md 的 5 个用户故事与关键边界情况。执行前需已完成实现阶段（`/speckit-tasks` + `/speckit-implement`）。

## 前置条件

1. 安装新增依赖（见 [research.md](./research.md) R-001~R-004）：

   ```bash
   pnpm add @xyflow/react elkjs framer-motion html-to-image
   ```

2. 准备一个可连接的 PostgreSQL 实例，至少包含：
   - 一个有若干张表且表之间存在外键关联的数据库（验证核心路径）。
   - 一个有表但没有任何外键关联的数据库（验证 FR-015 边界）。
   - 一个没有任何表的数据库（验证 FR-014 边界）。

3. 启动开发环境：

   ```bash
   pnpm dev
   ```

## 场景 1：从连接管理面板入口进入（User Story 1，P1）

1. 在连接管理面板中建立并展开一个连接，展开其下的业务数据库列表。
2. 左键点击目标数据库，确认弹出下拉菜单且包含「ER 分析」选项。
3. 点击「ER 分析」，确认打开一个新标签页，标题包含该数据库名称。
4. 确认画布在数据加载完成后展示表节点与外键连接线（对照 [data-model.md](./data-model.md) 一节的字段，确认列名/数据类型/主键标识均可见）。
5. 再次对同一数据库重复步骤 2-3，确认系统切换到已存在的标签页，而不是新建一个（FR-009）。

## 场景 2：从侧边栏悬浮面板入口进入（User Story 2，P1）

1. 切换到 Work 模式，点击左侧侧边栏「ER 分析」入口，确认弹出居中悬浮面板。
2. 在连接下拉列表中直接选择一个已连接的连接，确认面板展示该连接下的业务数据库列表。
3. 关闭面板重新打开，改为在输入框中输入连接的部分名称，确认列表按输入内容筛选。
4. 输入一个不存在的连接名称，确认提示「未找到匹配连接」且无法继续选择数据库（Edge Case）。
5. 选中一个数据库，确认面板关闭且打开对应 ER 分析标签页，效果与场景 1 一致（FR-008）。

## 场景 3：核心可视化能力（User Story 3，P1）

1. 打开一个包含外键关联的数据库的 ER 分析标签页。
2. 确认每张表是独立节点，展示表名、列名、数据类型、主键标识（对照 SC-003：无需打开额外详情即可分辨主键）。
3. 确认有外键关系的两表之间存在连接线（对照 SC-002：数据库中定义的外键 100% 呈现，可用 `psql` 执行 `\d+ <table>` 或查询 `information_schema.table_constraints` 交叉核对数量）。
4. 确认首次打开即为无重叠的自动布局（FR-012），无需手动拖拽即可读。
5. 用鼠标滚轮缩放、拖拽空白区域平移、拖拽某个表节点移动位置，确认三种交互均流畅无卡顿（对照 SC-005，可用 ≤100 表的数据库测试）。

## 场景 4：多标签页独立状态（User Story 4，P2）

1. 打开数据库 A 的 ER 分析标签页，拖拽调整若干表节点位置。
2. 打开数据库 B 的 ER 分析标签页（通过任一入口）。
3. 切换回数据库 A 的标签页，确认此前调整的节点位置仍保持（FR-018，对照 SC-006）。
4. 关闭数据库 B 的标签页，确认数据库 A 的标签页与其对应连接不受影响（FR-017）。

## 场景 5：导出图片（User Story 5，P3）

1. 在已加载完成的 ER 分析标签页中，点击工具栏导出按钮。
2. 确认生成的图片文件包含当前画布上的表节点与连接线。

## 边界与异常场景

| 场景 | 验证步骤 | 期望结果 |
|---|---|---|
| 空数据库（无表） | 对无表数据库触发 ER 分析 | 显示「该数据库暂无表」空状态提示，非空白画布（FR-014） |
| 有表无外键 | 对该数据库触发 ER 分析 | 所有表以网格布局展示，并提示未发现关联关系（FR-015） |
| 加载中重复触发 | 加载尚未完成时再次通过入口触发同一目标 | 显示加载状态指示，不重复发起请求/不产生重复标签页 |
| 查询失败 | 断开网络或使用无权限账号连接后触发 | 标签页内显示可读错误信息，并提供重试按钮（FR-016） |
| 连接被手动断开 | ER 分析标签页打开期间，在连接管理面板中断开该连接 | 标签页提示连接已断开，不展示过期/错误数据 |
| 非 PostgreSQL 类型（预留） | 当前系统仅有 PostgreSQL，可跳过；未来新增适配器后需验证 | 未实现 `getErDiagramData` 的驱动类型触发时，提示「当前数据库类型暂不支持 ER 分析」（FR-020） |

## 收尾检查

```bash
pnpm typecheck
pnpm lint
```

两者均需通过，确认新增类型（`ipc.ts`/`workspace.ts`/`components/er/types.ts`）与现有代码无类型冲突，且新文件符合项目 ESLint 规则。

## 验证记录（T034）

> 本次验证方式：**代码路径静态核对**（逐条对照 5 个场景与边界表格追踪实际实现代码），非真实 PostgreSQL 实例 + 界面交互的现场走查——当前执行环境无可用的图形界面与数据库实例。建议在具备真实连接的开发环境中按上述步骤补充一次现场走查。

| 场景 | 结果 | 依据 |
|---|---|---|
| 场景 1 入口/去重 | 通过 | `DatabaseNode.tsx` 下拉菜单「ER 分析」→ `workspaceStore.openErAnalysisTab` 按 `connectionId+database` 去重复用标签页（FR-009） |
| 场景 2 悬浮面板筛选 | 通过 | `ERPickerPanel.tsx` 连接下拉 + 名称筛选，未匹配时提示「未找到匹配连接」并禁止继续；选中数据库后复用 `openErAnalysisTab` |
| 场景 3 核心可视化 | 通过 | `ERTableNode` 展示列名/类型/主键图标；`EREdge` 渲染外键连线；`ERLayoutEngine.computeLayout`（elkjs layered）计算初始无重叠布局；`ReactFlow` 原生支持滚轮缩放/平移/节点拖拽 |
| 场景 4 多标签页独立状态 | 通过 | `erStore.nodePositions`/`isLayouting` 按 `tabId` 隔离；`workspaceStore.closeTab/closeOtherTabs/closeAllTabs` 才调用 `clearTabState`（本次实现过程中修复了此前误在标签页切换时清理的回归） |
| 场景 5 导出图片 | 通过 | `ERToolbar` 导出按钮 → `ERDiagram.handleExport` 用 `getNodesBounds`/`getViewportForBounds` + `html-to-image` 生成 PNG |
| 边界：空数据库 | 通过 | `tableCount === 0` 展示「该数据库暂无表」（FR-014） |
| 边界：有表无外键 | 通过 | `foreignKeyCount === 0` 展示「未发现关联关系」提示；`computeLayout` 对无边图仍产出无重叠布局 |
| 边界：加载中重复触发 | 通过 | 标签页去重避免重复挂载；`load()` 内 `requestSeqRef` 防止并发请求竞态覆盖 |
| 边界：查询失败 | 通过 | `getSchemas`/`getErDiagramData` 异常被捕获，展示错误信息与「重试」按钮（FR-016） |
| 边界：连接被手动断开 | 部分通过 | 无专门的「连接已断开」文案，但复用现有错误态展示驱动/IPC 层抛出的错误信息，不展示过期数据；与项目其他标签页（query/table）对断连的处理方式一致，非本功能引入的缺口 |
| 边界：非 PostgreSQL 类型 | 通过 | `DriverManager.getErDiagramData` 检测驱动未实现该方法时抛出「当前数据库类型暂不支持 ER 分析」（FR-020） |
